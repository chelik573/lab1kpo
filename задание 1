#include <iostream>
#include <cmath>
using namespace std;
int main() {                        \\ изменение 
	setlocale(LC_ALL, "RU");
	float n, m, a;

	cin >> n >> m >> a;

	if (n <= 0 || m <= 0 || a <= 0) {
		cout << "введите значения больше " << endl;
	}
	else {
		 int bok1= ceil (n/a);
		 int bok2 = ceil (m/a);
		 int plit = bok1 * bok2;
		cout << "нужное количество плит: " << plit  << endl;
	}
	return 0;
}
